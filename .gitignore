# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
dist/
build/
*.egg-info/
*.egg
.eggs/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/
*.pyo

# uv
uv.lock
# keep sdk/uv.lock and twin/uv.lock — they ARE committed

# ── Node ─────────────────────────────────────────────────────────────────────
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
.pnpm-debug.log*

# ── Rust / Anchor ────────────────────────────────────────────────────────────
target/
**/*.rs.bk
.anchor/
# Keep deployed.json (pinned program IDs)
!programs/deployed.json

# ── Solana ───────────────────────────────────────────────────────────────────
test-ledger/
*.so.deploy
# Keypairs — never commit private keys
*.json.bak

# ── Environment / Secrets ────────────────────────────────────────────────────
.env
.env.local
.env.*.local
.env.development
.env.production
# Keep .env.example files
!**/.env.example

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ───────────────────────────────────────────────────────────────────
.idea/
*.swp
*.swo
*~
# Keep devcontainer config
!.devcontainer/

# ── LaTeX build artefacts ────────────────────────────────────────────────────
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.pdf
*.listing

# ── Internal docs / Grant / Research (private, not for public repo) ──────────
CLAUDE.md
PRE_SHIP_CHECKLIST.md
STIE_GRANT_APPLICATION.md
STIE_GRANT_APPLICATION.tex
AUXIN_AUTOMATA_DEEP_DIVE.md
auxin_automata_grant_application.tex
auxin_automata_overview.tex
auxin_automata_phases.tex
auxin_automata_prompts.tex
auxin_financial_intelligence_plan.tex
scripts/seed_machine_treasury.ts

# ── Misc ─────────────────────────────────────────────────────────────────────
*.lock
!pnpm-lock.yaml
!Cargo.lock
